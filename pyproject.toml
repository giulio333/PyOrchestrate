[project]
name = "PyOrchestrate"
version = "0.1.0"
description = "PyOrchestrate"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "pyzmq>=26.4.0",
    "requests>=2.32.3",
]

[project.scripts]
pyorchestrate = "PyOrchestrate.cli:main"