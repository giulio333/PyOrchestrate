# documentazione: artifact JSON per la SDK reference di Mintlify
# build: python -m sphinx -b json sphinx docs/sdk-artifacts/sphinx-output
sphinx==8.1.3

flake8
flake8-gl-codeclimate
pytest
coverage
pylint

# La CI verifica la formattazione con `black --check`: una nuova major di black
# può cambiare lo stile e far fallire il job senza che il codice sia cambiato.
# Se succede, fissare qui la versione usata dal workflow.
black
